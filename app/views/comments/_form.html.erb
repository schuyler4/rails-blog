<%= form_for([@article, @article.comments.build]) do |f| %>
  <div>
    <%= f.label :commenter %>
    <%= f.text_field :commenter %>
  </div>

  <div>
    <%= f.label :body %>
    <%= f.text_area :body %>
  </div>

  <div>
    <%= f.submit %>
  </div>
<% end %>
